# Database (Neon Postgres)
DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"

# Inngest (for background jobs)
# Local development: use "local" for both
# Production: get from https://app.inngest.com
INNGEST_EVENT_KEY="your-event-key"
INNGEST_SIGNING_KEY="your-signing-key"

# Next.js
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional: Upstash Redis (for rate limiting in production)
# UPSTASH_REDIS_REST_URL="https://..."
# UPSTASH_REDIS_REST_TOKEN="..."

# Authentication (NextAuth.js)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-random-secret-key-use-openssl-rand-base64-32"

# Google OAuth (for authentication and Gmail API)
# Get these from Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-client-secret"
GOOGLE_REDIRECT_URI="http://localhost:3000/api/auth/callback/google"

# Vercel Blob Storage (for file uploads)
# Get from Vercel Dashboard: Project Settings > Storage
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxxxx"

# Optional: Other OAuth credentials for adapters
# HUBSPOT_API_KEY="..."
# LINKEDIN_CLIENT_ID="..."
# LINKEDIN_CLIENT_SECRET="..."
