/**
 * Pathfinding Constants
 */

/** Maximum number of hops in a path */
export const MAX_HOPS = 3;

/** Maximum number of paths to return */
export const MAX_PATHS = 5;

/** Default score for edges without explicit scores */
export const DEFAULT_EDGE_SCORE = 0.5;

/** Hop penalty factor (applied per hop beyond the first) */
export const HOP_PENALTY = 0.9;
